<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.highfive.chajiserver.mapper.UserCarMapper">

    <select id="countByMemberIdx" resultType="int">
        SELECT COUNT(*) FROM user_car WHERE member_idx = #{memberIdx}
    </select>

    <insert id="insertUserCar" parameterType="com.highfive.chajiserver.dto.CarRegistDTO" useGeneratedKeys="true" keyProperty="userCarIdx">
        INSERT INTO user_car (member_idx, car_idx, car_nickname)
        VALUES (#{memberIdx}, #{carIdx}, #{carNickname})
    </insert>

</mapper>
